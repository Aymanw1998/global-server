PORT=5000
MONGO_URI=your_mongo_connection_string
FILE_STORAGE_ROOT=/home/wahbani/storage

# On the SERVER PC, replace SERVER_PC_IP_OR_DOMAIN with the real IP/domain.
AI_SERVICE_URL=http://SERVER_PC_IP_OR_DOMAIN:7000

SKIP_DB_CONNECT= #true or false, set to true to skip DB connection (useful for testing without a DB)
